# ── Зависимости и собранные артефакты ────────────────────────────────
node_modules/
dist/
out/

# ── Скомпилированные нативные хелперы (пересобираются билдом) ────────
src/native/key-helper
src/native/ocr-helper

# ── ОС / редактор ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo

# ── Логи ─────────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Личные настройки редактора (но не общие .vscode/*.json) ──────────
.vscode/.ropeproject
.vscode/settings.json

# ── Временные файлы OCR ──────────────────────────────────────────────
te-snip-*.png

# ── Claude Code (приватная история чатов и project config) ─────────
.claude/

# ── Внутренние dev-заметки (не для публикации) ─────────────────────
FIX_*.md
